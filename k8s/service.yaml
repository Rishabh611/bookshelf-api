apiVersion: v1
kind: Service
metadata:
  name: bookshelf-api-service
spec:
  type: LoadBalancer
  selector:
    app: bookshelf-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080