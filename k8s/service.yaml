apiVersion: v1
kind: Service
metadata:
  name: bookshelf-api-service
spec:
  type: NodePort
  selector:
    app: bookshelf-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080